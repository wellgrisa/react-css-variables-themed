
name: CI

on: [push]

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: yarn 
        run: yarn
        
      - name: lerna bootstrap
        run: yarn bootstrap

      - name: css-style-declaration test 
        run: yarn css-style-declaration test
